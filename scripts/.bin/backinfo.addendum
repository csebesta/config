#!/bin/bash

# Ideas and notes regarding creation of info file for quicker reference
# Needs an option to override (i.e. Run without file)

if [ -f "/etc/backinfo/config" ]; then
	source "/etc/backinfo/config"
else
	exit
fi
